[SETTING]
    pack_path = string(default=~/.vim/pack)
[REPOSITORY]
    [[__many__]]
        package = string(default=vimpck)
        type = string(default=start)
        freeze = boolean(default=False)
